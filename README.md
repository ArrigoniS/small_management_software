# small_management_software
Development of a small management software for a new vegan store.  The program is written entirely in **Python**, using only the **standard library** (no external dependencies).   It is designed to be run directly from the **command line** and provides an intuitive menu-based interface.
